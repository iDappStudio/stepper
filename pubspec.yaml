name: stepper
description: iDapp Studio
version: 1.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.18.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
